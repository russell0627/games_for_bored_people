import 'package:flutter/material.dart';

class EndlessDungeonPage extends StatelessWidget {
  const EndlessDungeonPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Endless Dungeon!"),),
    );
  }
}
